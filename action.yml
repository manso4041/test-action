name: 'Test Action'
description: 'A simple test action for PoC'
runs:
  using: 'composite'
  steps:
    - run: |
        echo "ACTION COMPROMISED - Tag v1 has been poisoned!"
        echo "If this were real, I could steal secrets like GITHUB_TOKEN, AWS keys, API keys..."
        echo "Dumping env to prove access:"
        env | grep -i "GITHUB_\|RUNNER_\|CI" | head -10
      shell: bash
